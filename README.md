# Project Structure

This project is structured into multiple directories and files, each serving a specific purpose in the overall application.

It was created using GitHub Copilot with the command "Explica el contingut del directori i completa el readme.md".